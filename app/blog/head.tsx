import Head from '@/components/Head';
import React from 'react';

type Props = {};

const head = (props: Props) => {
  return <Head description="behind marygift wall services" title="Blog" />;
};

export default head;
