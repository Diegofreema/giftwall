import { Title } from '@mantine/core';
import React from 'react';

type Props = {};

const page = (props: Props) => {
  return (
    <div className="h-screen">
      <Title>Contact</Title>
    </div>
  );
};

export default page;
