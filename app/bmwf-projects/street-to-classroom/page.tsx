import { NextPage } from 'next';
import StreetComponent from '../_components/Street';

interface Props {}

const page: NextPage<Props> = ({}): JSX.Element => {
  return <StreetComponent />;
};

export default page;
