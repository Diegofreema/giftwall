import Head from '@/components/Head';
import React from 'react';

type Props = {};

const head = (props: Props) => {
  return <Head description="behind marygift wall gallery" title="Gallery" />;
};

export default head;
