import Head from '@/components/Head';
import React from 'react';

type Props = {};

const head = (props: Props) => {
  return <Head description="behind marygift wall events" title="Events" />;
};

export default head;
